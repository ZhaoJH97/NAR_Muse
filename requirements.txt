# Core training / inference deps for the LaDA-Band V2A model.
# (Audio encode/decode with MuCodec uses the separate Muse/requirements_mucodec.txt env.)
torch>=2.1
transformers==4.57.3
pyyaml>=6.0
numpy>=1.24
tqdm>=4.65
